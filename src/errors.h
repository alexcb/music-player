#pragma once

#define MAX_ERROR_STR_SIZE 1024

typedef enum
{
	OK = 0,
	UNKNOWN_ERROR,
	OUT_OF_MEMORY,
	MUTEX_ERROR,
	FILESYSTEM_ERROR,
	ERROR_UNEXPECTED_CHAR,
	ICY_PARSE_ERROR,
	SOCK_SEND_ERROR,
	NOT_FOUND
} errors;
